# WindowRanger 1.0.6

WindowRanger 1.0.6 is a focused window-management reliability update for the Stable release.

## Fixed

- Fixed-size Finder operation windows, including copy progress, no longer enter Tiled or Accordion
  layouts or displace ordinary Finder windows.
- A single rejected resize no longer causes an ordinary application window to be incorrectly
  treated as fixed-size and removed from managed layouts.
- Quick Apps retain their Shelf ownership across brief Accessibility omissions instead of
  occasionally joining whichever workspace is active when they reappear.
- ChatGPT's transient update-check surfaces no longer enter workspace membership or briefly reflow
  its main window.

## Requirements

- macOS 14 or later.
- Accessibility permission for window discovery and management.
- Screen Recording permission only when optional captured workspace previews are enabled.

## Installation

Download `WindowRanger-1.0.6.dmg` from this release's **Assets** section, open it, and drag
WindowRanger onto the Applications shortcut. The notarized ZIP and both checksum files are included
as fallbacks and for verification.

Homebrew users can install or upgrade the public Stable Cask with:

```sh
brew install --cask appranger/tap/windowranger
brew upgrade --cask appranger/tap/windowranger
```

Existing Stable users can also install this update through WindowRanger's signed in-app updater once
the 1.0.6 feed is published.

## Known limitations

- WindowRanger manages virtual workspaces inside the current macOS desktop; it does not replace
  native macOS Spaces.
- Captured workspace previews remain optional and off by default.
- Some applications expose ambiguous or read-only Accessibility windows that WindowRanger
  conservatively ignores.

## Feedback and security

Use the repository issue forms for privacy-safe bugs, feature requests, and questions. Do not post
suspected vulnerabilities, private diagnostics, window titles, document names, URLs, or user file
paths in a public issue; follow `SECURITY.md` instead.
