# WindowRanger brand assets

This directory is the durable source library for WindowRanger artwork. Generated
explorations are retained so future hero images, illustrations, poses, and icon
variants can reuse the same visual history instead of starting from scratch.

## Character assets

- `characters/exploration/` contains every ranger direction explored before the
  robot was selected.
- `characters/canonical/windowranger-robot.png` is the current identity anchor.
- `characters/canonical/turnaround.png` records the front, side, back, and top
  views.
- The other canonical images are consistency tests for poses and scenes.

The character invariants are a warm-ivory rounded-square body, one offset cobalt
rear window panel, cobalt helmet dome, broad midnight ranger brim, black visor,
two cyan dot eyes with no mouth, navy limbs and boots, and an amber four-point
star antenna.

## Icon assets

- `icons/concepts/` retains all three icon families in Default and Dark
  appearances.
- `icons/production/source/` contains normalized 1024 x 1024 masters for the
  selected Helmet Signal direction.
- `icons/exports/macos/` is generated by `scripts/generate-app-icons.sh` and
  contains Default, Dark, and Mono iconsets plus ICNS files.
- `Sources/Resources/AppIcon.icon` is the editable Icon Composer source used by
  current Xcode versions. It keeps the artwork unmasked so macOS can apply the
  appropriate system shape, depth, lighting, and appearance treatment.
- `Sources/Resources/Assets.xcassets/AppIcon.appiconset/` is the build input for
  the deterministic raster fallback and older macOS icon sizes.

Run the exporter after changing a production master:

```sh
./scripts/generate-app-icons.sh
```

The Icon Composer document is the modern source. The raster app-icon set remains
useful for the macOS 14 deployment target, exact size inspection, and as a
deterministic fallback. Dark and Mono 1024-pixel masters are intentionally kept
even though Icon Composer can derive those system appearances from the source.

## Provenance

The artwork in this directory was generated for WindowRanger with OpenAI's
built-in image generation workflow on 9 August 2026. The selected icon direction
was tested as a 16-pixel raster before export. See `icons/manifest.json` for the
machine-readable inventory and production decision.
